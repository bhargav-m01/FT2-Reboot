# FT2-Reboot

1) create a virtual env and install requirements.txt file

2)Run the following DB commands to create a DB with the product sme table.

flask db init
flask db migrate
flask db upgrade

3) added the admin panel as well and can be access thorugh /admin route.
